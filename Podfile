use_frameworks!

platform :ios, '9.0'

target 'newProject_Example' do
  pod 'newProject', :path => '../'

  target 'newProject_Tests' do
    inherit! :search_paths

    pod 'FBSnapshotTestCase'
  end
end
